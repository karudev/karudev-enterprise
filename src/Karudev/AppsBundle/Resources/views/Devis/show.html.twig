{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Devis</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Projectname</th>
                <td>{{ entity.projectName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Datecreated</th>
                <td>{{ entity.dateCreated|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Datelastupdated</th>
                <td>{{ entity.dateLastUpdated|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Amountht</th>
                <td>{{ entity.amountHt }}</td>
            </tr>
            <tr>
                <th>Amountttc</th>
                <td>{{ entity.amountTtc }}</td>
            </tr>
            <tr>
                <th>Methodpayment</th>
                <td>{{ entity.methodPayment }}</td>
            </tr>
            <tr>
                <th>Regulationprovided</th>
                <td>{{ entity.regulationProvided }}</td>
            </tr>
            <tr>
                <th>Deliverytime</th>
                <td>{{ entity.deliveryTime }}</td>
            </tr>
            <tr>
                <th>Deliverytimeunit</th>
                <td>{{ entity.deliveryTimeUnit }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('devis') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('devis_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
